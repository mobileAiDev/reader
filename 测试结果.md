# 性能优化测试结果

日期：2026-07-26

结论：优化完成并通过真机验证。`Map<String, Double>` 是 Java 堆过高的主要原因之一，但不是全部原因；ONNX Session 永久持有和 V8 标记摘要全量反序列化也是独立来源。

## 回答核心问题

原实现的 512 条 LRU 缓存中，每条 BGE 向量有 512 个维度。将这些维度存成 `LinkedHashMap<String, Double>` 会为 key 字符串、Map entry、装箱 Double、哈希桶和对象对齐付出大量额外开销：

- 512 条 Map 向量约 `30.6 MiB`
- 改为逻辑等价的 `DoubleArray` 后，512 条数值载荷约 `2 MiB`
- 单这一项约减少 `28.6 MiB`

没有直接改成 `FloatArray`，因为 Float 会产生二次舍入，无法承诺“完全等价”。当前实现以 `DoubleArray` 保存同一批 Double 数值，并按原顺序计算余弦。

## 真机结果

测试设备为 OnePlus PKR110（Android 16 / API 36），包名 `com.ldp.reader`，使用同一本《苟在两界修仙》和同一 V8 动态范围。

| 核心结果 | 基线 | 优化版 |
| --- | ---: | ---: |
| 分析章 / 目标章 | `325 / 320` | `325 / 320` |
| 正常 / 错章 / 不确定 | `150 / 170 / 0` | `150 / 170 / 0` |
| 连续错章起点 `badTail` | `440` | `440` |
| 内容摘要 | `d12e3f2dc25e` | `d12e3f2dc25e` |
| 完成事件 `heapUsed` | `144 MB` | `53 MB` |
| trim 后 Java Heap PSS | `129712 KB` | `32168 KB` |
| trim 后 Native Heap PSS | `104764 KB` | `61144 KB` |
| trim 后 TOTAL PSS | `395248 KB` | `269151 KB` |

优化版相对基线：

- 完成事件 Java 已用堆减少 `91 MB`（约 `63.2%`）
- 稳定 Java Heap PSS 减少 `75.2%`
- Native Heap PSS 减少 `41.6%`
- TOTAL PSS 减少 `31.9%`

冷启动 Java Heap PSS 为 `42892 KB → 42320 KB`，基本不变，说明收益集中在 V8 分析和分析后的常驻对象，而不是通过改变普通阅读 UI 获得。

## 逻辑等价证据

1. 初始输入均为 `analysis=170, targets=160, chars=440002`。
2. 扩展输入均为 `analysis=325, targets=320, chars=1485583`。
3. 最终状态、连续边界和内容摘要完全一致。
4. 基线与优化版缓存 JSON 去掉创建时间和诊断耗时后，规范化 SHA-256 均为：
   `6800e9c4a2fe29144e562366526cfed068aeaa9d3c2d8f17c617d1110806dc34`
5. 单测逐 bit 验证 dense/dense 和 dense/Map 的 Double 余弦结果。
6. 磁盘 V1 格式未改变，真实 ONNX 写入后可由新实例命中磁盘并得到相同向量。

## 已完成修改

1. BGE 稠密向量使用 `DoubleArray` 后端，保留原 Map 接口。
2. 512 项 LRU 保留，并记录条目数和真实数值载荷。
3. ONNX 模型在活动批次内复用，空闲 60 秒后关闭。
4. V8 标记摘要使用流式 JSON 读取，跳过大体积 fingerprints。
5. 磁盘缓存格式不变，旧缓存继续使用。
6. 磁盘淘汰去掉反复 `removeAt(0)`。

## 性能说明

优化版扩展阶段耗时为 `211332 ms`，基线为 `553961 ms`。优化版使用了基线已写入的磁盘向量：

- `memHits`：两者都是 `27885`
- 基线：`diskHits=11939, onnxRuns=1840`
- 优化版：`diskHits=13779, onnxRuns=0`

因此这次耗时下降主要证明磁盘缓存路径有效，不能把全部时间收益归因于数组化。内存收益则由满载 512 项 LRU 的真机事件、分阶段 heap/meminfo 和空闲释放事件直接证明。

## 验证结果

- `:source-engine:test`：146 tests，0 failures，1 skipped
- 新增稠密向量测试：4 tests，0 failures
- V8 标记缓存测试：27 tests，0 failures
- `:app:assembleDebug`：通过
- App 全量单测：578 tests，576 passed，1 skipped，1 个与 V8 无关的既有失败
- AI App Bridge：安装、ReadActivity、阅读截图、events、模型释放均通过
- 最终 APK 重装后恢复 `marks=320, hidden=170`，事件确认 cache current 并跳过重复 V8 计算

唯一既有失败是测试反射旧版双参数 `isSameTitleCandidate`，而工作树中的实现已改为三参数。本次没有修改这段用户已有业务改动。

最终真机截图：
`app/build/ai_app_bridge_artifacts/v8-memory-optimization/ai_app_bridge_screenshot-20260725-173915-504-69395-3u1517.png`

APK：

- 路径：`app/build/outputs/apk/debug/app-debug.apk`
- 大小：`67655972 bytes`
- SHA-256：`3dd7f3562af9e58c4bb3dd259d28b5f32c0bd1b1f04ce9bd21eeae2998a4382f`

## 最终判断

不需要为了这次内存问题先训练“整章模型”。现有 BGE 的主要问题不是 512 维本身，而是把固定维度稠密数组错误地对象化成 Map，再叠加永久 Session 和大 JSON 全量读取。

目前已在不改变判定逻辑的前提下解决这三个问题。后续若训练整章级错章模型，应把它作为减少推理次数和简化特征管线的独立项目，而不是这次内存修复的必要条件。

## 主线程 I/O 优化阶段 1 结果

设置页卡顿已经由真机事件时间确认，不是动画或主观感觉：

- 修改前连续 7 次首帧范围：`1088–1189 ms`，中位数 `1170 ms`
- 修改后连续 7 次首帧范围：`46–70 ms`，中位数 `50 ms`
- 首帧中位数减少 `1120 ms`，即 `95.7%`

根因是设置页首帧前在主线程递归统计缓存两次。现在缓存统计在
`Dispatchers.IO` 执行，UI 首帧不再等待扫描；异步完成后仍显示相同的
`340.44 M`，开关状态、页面结构和返回流程保持不变。

证据：

- 修改前计时：`build/ai_app_bridge_artifacts/main-thread-io-baseline/settings-launch-before.txt`
- 修改后计时：`build/ai_app_bridge_artifacts/main-thread-io-baseline/after-cache-offload/settings-launch-after.txt`
- 修改后截图：`build/ai_app_bridge_artifacts/main-thread-io-baseline/after-cache-offload/ai_app_bridge_screenshot-20260726-034405-506-42843-7o3mbo.png`
- 修改后 UI tree：`build/ai_app_bridge_artifacts/main-thread-io-baseline/after-cache-offload/tree.xml`
- App 全量单测：583 tests，0 failures，0 errors，1 existing skip
- Debug APK 构建、AI App Bridge 安装和真机回归均通过

## 主线程 I/O 优化阶段 2 结果

本机导入页原先在首帧前同步枚举当前目录。修改后目录遍历由
`viewLifecycleOwner.lifecycleScope` 启动并在 `Dispatchers.IO` 执行；
进入新目录或返回上一级会先取消旧任务，避免较慢的旧结果覆盖当前路径。

- 修改前连续 7 次首帧：`119, 95, 97, 95, 109, 114, 107 ms`，中位数 `107 ms`
- 修改后连续 7 次首帧：`102, 69, 74, 73, 70, 74, 68 ms`，中位数 `73 ms`
- 首帧中位数减少 `34 ms`，即 `31.8%`
- StrictMode 复测不再出现 `listVisibleChildren` 和 `importFileFilter` 主线程栈
- 快速进入 Android 目录并立即返回后，路径和列表保持根目录，旧异步结果未串页
- `FileImportPerformanceContractTest` 已完成红灯到绿灯
- `FileSystemUiResourceContractTest` 通过

真机截图：
`build/ai_app_bridge_artifacts/main-thread-io-audit/file-import-final/ai_app_bridge_screenshot-20260726-042447-947-50696-48anlc.png`

最终 Gate：

- App 全量单测：584 tests，0 failures，0 errors，1 existing skip
- `:app:assembleDebug`：通过
- 最终 APK 大小：`67663563 bytes`
- 最终 APK SHA-256：`4f2db206db83a0d20565ea2a81f4b97c28d5d1fe15af1f27809896401b8f4542`
- AI App Bridge 重装、手机目录显示、快速进入/返回和进程日志检查：通过

列表行绑定时的单文件元数据读取仍可继续优化，但它不属于本阶段的目录枚举改动，
会按“每一部分测试并提交后再做下一部分”的约束单独处理。

## V8 速度优化阶段 1 结果

固定书籍的多个目标章共用同一组参考章节。旧实现会对每个目标章
重建参考 BGE 窗口，并重复计算参考窗口间的余弦校准。现在仅保留
1 份完全相同的参考 bundle；参考文本或窗口配置变化时立即失效。

固定《苟在两界修仙》的真机证据：

- 输入一致：`analysis=264, targets=256, chars=1031714`
- 输出一致：`normal=84, wrong=172, inconclusive=0`
- 内容摘要一致：`6e04c246a1be6f21bf2ab037fb122039`
- 三次优化运行的规范化缓存 SHA-256 均为
  `f5dbab99f03f14a3e2cb468f07109f8548572030bd4765347a32c9f89808239f`
- 128 目标章阶段中，语义内存命中调用由 `10809` 降到 `864`，减少 `92.0%`
- 历史无监控的同批 256 章耗时和 `155522 ms`，优化版无内存轮询样本
  `148002 ms`，方向性改善 `4.84%`
- 已热过的后 128 章在三次样本中稳定改善 `1.88%–2.66%`

这项优化对 detector 有小幅且方向一致的收益，但整本 epoch 仍是
`417–441 s` 量级，不应宣称已显著改善整本速度。下一个主瓶颈是：

1. 三个动态阶段间串行获取章节内容，本次固定书籍累计超过 `220 s`。
2. 每阶段空闲 60 秒后释放 BGE 模型，后续阶段需从约 5 万个 4 KiB 独立文件中重新读取冷嵌入。
3. 高频 `dumpsys meminfo` 本身会触发 Android 显式 GC，后续速度 A/B 必须与内存峰值采样分开。

本阶段的高频内存采样中，优化版最大 TOTAL PSS 为 `511050 KB`和
`564856 KB`，基线为 `632855 KB`。这足以排除新的峰值回归，但不把差值宣称为本修改的独立内存收益。

验证 Gate：

- `V8ReferenceSemanticCacheTest`：同参考复用、逐 bit 等价、失效和单条目容量通过
- `:source-engine:test`：通过
- App 全量单测：584 tests，0 failures，0 errors，1 existing skip
- `:app:assembleDebug`：通过
- AI App Bridge 真机 FULL 运行三次，无崩溃，结果逐章一致

## V8 速度优化阶段 2 结果

余弦 Top-K 聚合不再为每个窗口构造并完整排序 `List<Double>`；
现在以最大 8 项的 `DoubleArray` 保持相同降序 Top-K 和相同求和顺序。

固定《苟在两界修仙》的效果等价性：

- 输入仍为 `analysis=264, targets=256, chars=1031714`。
- 输出仍为 `normal=84, wrong=172, inconclusive=0`。
- `contentDigest` 仍为 `6e04c246a1be6f21bf2ab037fb122039`。
- 忽略 `createdAtMs` 和 reason 中 `ms=...` 后，基线、阶段 1、阶段 2
  三份完整 JSON 的规范化哈希相同。
- `V8TopMeanTest` 对 1,000 组随机/边界输入逐 bit 对齐旧排序实现。

无 `meminfo` 轮询的同机 A/B：

- 最终 256 章逐章耗时和：`148002 ms -> 139634 ms`，降低 `5.65%`。
- 已热后 128 章：`55882 ms -> 54520 ms`，降低 `2.44%`。
- 整本 epoch：`428553 ms -> 403040 ms`，本次降低 `5.95%`。
- 128 章 detector：`33031 ms -> 32984 ms`，基本持平。

整本时间包含网络取章波动，所以 `5.95%` 是本次实测而非稳定承诺；
`5.65%` 的 detector 逐章耗时改善更能反映本修改方向。

内存方面，本阶段确定消除了热点中的装箱分数列表、完整排序副本和截取列表，
但没有用会触发显式 GC 的高频 PSS 轮询。完成后单次 `TOTAL PSS=303287 KB`，
只作为安全快照，不作为峰值降幅结论。

验证 Gate：

- `:source-engine:test`：150 tests，0 failures，0 errors，1 existing skip
- App 全量单测：584 tests，0 failures，0 errors，1 existing skip
- 正常 READING_LIGHT APK 构建、安装、启动成功
- AI App Bridge 首页截图/tree 正常，应用日志 0 条异常
- 目标书 256 条缓存正常恢复，结果仍为 `84 / 172 / 0`
