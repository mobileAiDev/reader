# 性能优化测试进度

日期：2026-07-26

状态：进行中。V8 内存优化、主线程 I/O 阶段 1、2 已完成；
V8 速度阶段 1 已完成等价性与真机验证，下一步处理冷嵌入磁盘读取和串行章节取数。

## 目标

1. 在真实设备上区分 V8 的 Java 堆峰值、常驻 Java 堆、ONNX/native 内存和 UI/图形内存。
2. 找到主要内存来源，不以静态推测代替真机证据。
3. 只做判定逻辑等价的向量表示、LRU、磁盘缓存或生命周期优化。
4. 用相同书籍、相同 V8 范围复测判定结果、耗时和内存。

## 环境

| 项目 | 当前值 |
| --- | --- |
| 设备 | OnePlus PKR110，Android 16 / API 36，序列号 `b46093e6` |
| 包名 | `com.ldp.reader` |
| App | `1.0 (1)`，debuggable |
| AI App Bridge | 0.2.8，`status/tree/screenshot/events` 可用 |
| 基线书籍 | 《苟在两界修仙》 |
| 最终截图 | `app/build/ai_app_bridge_artifacts/v8-memory-optimization/ai_app_bridge_screenshot-20260725-173915-504-69395-3u1517.png` |
| APK | `app/build/outputs/apk/debug/app-debug.apk` |
| APK SHA-256 | `4f2db206db83a0d20565ea2a81f4b97c28d5d1fe15af1f27809896401b8f4542` |

## 根因结论

1. 512 个 512 维 BGE 稠密向量被表示成 `LinkedHashMap<String, Double>`，是 Java 堆被放大的主要原因之一：
   - Map 实测约 `30.6 MiB`
   - 等价 `DoubleArray` 数值载荷约 `2 MiB`
   - 单这一项理论减少约 `28.6 MiB`
2. ONNX Session 被永久单例持有，是 native 常驻内存的另一独立原因。
3. V8 标记目录约 `110787 KB`，旧摘要列表会完整反序列化每个 JSON 及大体积 fingerprints，形成额外维护峰值。
4. 160 目标章动态扩展到 320 目标章会放大上述问题，但切片数量不是最底层的数据结构根因。

## 已实施优化

1. BGE 稠密向量改为 `DoubleArray` 后端；历史 `Map<String, Double>` 契约和稀疏 n-gram 路径保持不变。
2. 余弦对稠密数组按旧实现的 Double 顺序累加，不使用 Float 二次舍入。
3. V1 磁盘缓存仍使用相同 magic/version/Double 序列，旧缓存可直接读取。
4. 512 项 LRU 保持不变，并增加 `memoryEntries`、`memoryValueKiB` 可观测字段。
5. V8 标记摘要改为流式读取，只解析 identity、source、createdAt 和 marks，跳过 fingerprints。
6. ONNX 模型按活动任务复用，空闲 60 秒关闭 Session 并丢弃内存 LRU。
7. 磁盘 LRU 淘汰改为线性下标遍历，消除排序后反复 `removeAt(0)` 的搬移。

## 真机 A/B

### 输入和结果

| 项目 | 基线 | 优化版 | 结论 |
| --- | ---: | ---: | --- |
| 初始 `analysis / targets / chars` | `170 / 160 / 440002` | `170 / 160 / 440002` | 相同 |
| 扩展 `analysis / targets / chars` | `325 / 320 / 1485583` | `325 / 320 / 1485583` | 相同 |
| `normal / wrong / inconclusive` | `150 / 170 / 0` | `150 / 170 / 0` | 相同 |
| `badTail` | `440` | `440` | 相同 |
| 内容摘要 | `d12e3f2dc25e` | `d12e3f2dc25e` | 相同 |
| 规范化标记 JSON SHA-256 | `6800e9c4…80f6dc34` | `6800e9c4…80f6dc34` | 相同 |

规范化哈希只忽略 `createdAtMs` 和 reason 中的运行耗时 `ms=...`；状态、置信度、建议、边界、evidence 和 fingerprints 均参与比较。

### 内存

| 阶段 | 基线 | 优化版 | 变化 |
| --- | ---: | ---: | ---: |
| 冷启动 Java Heap PSS | `42892 KB` | `42320 KB` | 基本不变 |
| 初始输入事件 `heapUsed` | `49 MB` | `31 MB` | `-18 MB` |
| 扩展输入事件 `heapUsed` | `129 MB` | `96 MB` | `-33 MB` |
| 完成事件 `heapUsed` | `144 MB` | `53 MB` | `-91 MB` |
| 完成后 Java Heap PSS | `128800~129712 KB` | `68708 KB` | 至少 `-60092 KB` |
| 同条件 trim 后 Java Heap PSS | `129712 KB` | `32168 KB` | `-75.2%` |
| 同条件 trim 后 Native Heap PSS | `104764 KB` | `61144 KB` | `-41.6%` |
| 同条件 trim 后 TOTAL PSS | `395248 KB` | `269151 KB` | `-31.9%` |

优化版事件确认 LRU 已满 `memoryEntries=512`，稠密数值载荷仅 `memoryValueKiB=2048`。空闲释放事件触发后，模型 Session 和这 512 个内存向量都不再常驻。

### 耗时和缓存

| 项目 | 基线 | 优化版 |
| --- | ---: | ---: |
| 扩展阶段总耗时 | `553961 ms` | `211332 ms` |
| `memHits` | `27885` | `27885` |
| `diskHits` | `11939` | `13779` |
| `onnxRuns` | `1840` | `0` |
| `diskWrites` | `1840` | `0` |
| `diskEvictions` | `1536` | `0` |

本次优化版复用了基线生成的磁盘向量，因此耗时下降证明磁盘缓存生效，但不能全部归因于数组表示。逻辑等价性由相同输入、相同最终摘要、相同结果和规范化 JSON 哈希证明。

## 四轮 QA

### Round 1：细节与单点逻辑

| 检查项 | 证据 | 结果 |
| --- | --- | --- |
| Map 契约 | 维度 key、entry、size 单测 | 通过 |
| Double 数值等价 | dense/dense、dense/map cosine 逐 bit 比较 | 通过 |
| 磁盘兼容 | 真实 ONNX 写 V1、第二实例磁盘读取 | 通过 |
| LRU 载荷 | 真机事件 `512 / 2048 KiB` | 通过 |

### Round 2：完整流程

| 流程 | 最终状态 | 结果 |
| --- | --- | --- |
| 冷启动→打开《苟在两界修仙》→初始 160→扩展 320→提交 | `normal=150, wrong=170, badTail=440` | 通过 |
| 提交后阅读 | `ReadActivity` 正常，章节可读，错章标识可见 | 通过 |
| 空闲 60 秒 | `source_v8_bge_model_released`，无崩溃 | 通过 |
| 最终 APK 重装→打开同书 | 恢复 `320` marks、`170` hidden，因 cache current 跳过重算 | 通过 |

### Round 3：稳健性

| 检查项 | 结果 |
| --- | --- |
| 旧 V1 磁盘缓存可直接读 | 通过 |
| 512 项 LRU 淘汰不改变结果 | 通过 |
| 连续动态扩展可释放并重建模型 | 通过 |
| 110 MB 标记目录维护扫描 | 流式摘要路径运行完成，维护周期正常结束 |

### Round 4：代码与构建

| Gate | 结果 |
| --- | --- |
| `:source-engine:test` | 146 tests，0 failures，1 opt-in skipped |
| `V8DenseBgeVectorTest` | 4 tests，0 failures |
| `SourceEngineV8MarkCacheTest` | 27 tests，0 failures |
| `:app:testDebugUnitTest` | 578 tests，576 passed，1 skipped，1 个既有非 V8 失败 |
| `:app:assembleDebug` | 通过 |
| AI App Bridge 安装与真机回归 | 通过 |

App 全量单测中唯一失败为
`fallbackMatchingKeepsSameTitleCandidatesWhenInitialAuthorIsWrong`：
测试仍反射旧的双参数 `isSameTitleCandidate(SourceBook, SourceBook)`，而工作树中已有实现是三参数版本。该差异在本次 V8 优化前已存在，本次未越界修改。

## 最终 Gate

- planned items: 4
- completed with evidence: 4
- V8 defects fixed: 3
- V8 unresolved defects: 0
- unrelated existing test failure: 1
- decision: V8 内存优化通过；判定逻辑等价，真机内存显著下降

## 主线程 I/O 阶段 1：设置页缓存统计

### 根因

`SettingsActivity` 在 `initWidget` 和首次 `onResume` 中各调用一次
`CacheUtils.getAppCacheSizeLabel`。该函数递归遍历内部和外部缓存目录；
当前设备约有 5 万个 V8 向量缓存文件，因此首帧前在主线程完整扫描两遍。

### 真机前后对比

| 指标（连续 7 次中位数） | 修改前 | 修改后 | 变化 |
| --- | ---: | ---: | ---: |
| SettingsActivity 首帧 | `1170 ms` | `50 ms` | `-95.7%` |
| `onCreate` | `518 ms` | `11 ms` | `-97.9%` |
| `onResume` | `613 ms` | `1 ms` | `-99.8%` |
| 缓存显示 | `340.44 M` | `340.44 M` | 相同 |

缓存遍历现由 `Dispatchers.IO` 执行，Activity 销毁时取消 UI 更新；
首次进入只触发一次异步刷新，不再在 `initWidget` 和 `onResume` 重复扫描。

### 证据矩阵

| Page/State | Entry | Screenshot | Tree | Expected | Observed | Verdict |
| --- | --- | --- | --- | --- | --- | --- |
| 设置页/修改前 | 我的→设置 | `build/ai_app_bridge_artifacts/main-thread-io-baseline/ai_app_bridge_screenshot-20260726-032058-619-33977-pm595e.png` | AI Bridge `SettingsActivity` tree | 记录卡顿基线 | 中位数 `1170 ms` | 复现 |
| 设置页/修改后 | 我的→设置 | `build/ai_app_bridge_artifacts/main-thread-io-baseline/after-cache-offload/ai_app_bridge_screenshot-20260726-034405-506-42843-7o3mbo.png` | `build/ai_app_bridge_artifacts/main-thread-io-baseline/after-cache-offload/tree.xml` | 首帧不等待目录扫描 | 中位数 `50 ms`，缓存最终显示 `340.44 M` | 通过 |

### Gate

- 回归测试先失败后通过：`SettingsPerformanceContractTest`
- UI/资源契约：`HomeUiResourceContractTest` 通过
- App 全量单测：583 tests，0 failures，0 errors，1 existing skip
- `:app:assembleDebug`：通过
- 连续打开/返回 7 次：通过
- 截图与 tree：设置项、开关和缓存值正常
- App 进程日志：未发现 `FATAL EXCEPTION`、`AndroidRuntime` 或 Settings 异常
- 清除缓存按钮：本阶段未执行，避免删除用户 340 MB 的有效缓存；代码路径未改变
- decision: 阶段 1 通过，可以独立提交

## 主线程 I/O 阶段 2：本机导入目录遍历

### 根因

`FileCategoryFragment.toggleFileTree` 在主线程同步调用
`LocalBookImportFiles.listVisibleChildren`。真机 StrictMode 记录显示首次打开本机导入时，
目录枚举和文件过滤分别占用主线程约 `65 ms`、`62 ms`，首帧必须等待遍历完成。

本阶段只把这一层目录枚举切到 `Dispatchers.IO`。列表绑定中的单文件元数据读取仍作为
独立后续项保留，没有和本次修改捆绑。

### 真机前后对比

| 指标（连续 7 次中位数） | 修改前 | 修改后 | 变化 |
| --- | ---: | ---: | ---: |
| FileSystemActivity 首帧 | `107 ms` | `73 ms` | `-31.8%` |
| 修改前范围 | `95–119 ms` | - | - |
| 修改后范围 | - | `68–102 ms` | - |

修改后 StrictMode 中不再出现
`LocalBookImportFiles.listVisibleChildren` 和 `importFileFilter` 的主线程磁盘访问栈。

### 证据矩阵

| Page/State | Entry | Screenshot | Expected | Observed | Verdict |
| --- | --- | --- | --- | --- | --- |
| 本机导入/最终 APK | 更多→本机导入→手机目录 | `build/ai_app_bridge_artifacts/main-thread-io-audit/file-import-final/ai_app_bridge_screenshot-20260726-042447-947-50696-48anlc.png` | 根目录异步加载，路径和计数正常 | 显示 `存储卡:/storage/emulated/0`，目录项与计数正常 | 通过 |
| 快速进入目录后立即返回 | Android→上一级 | AI App Bridge tree | 过期任务不能覆盖根目录 | 0.8 秒后仍为根路径且 Android 根目录项可见 | 通过 |

### Gate

- 回归测试先失败后通过：`FileImportPerformanceContractTest`
- UI/资源契约：`FileSystemUiResourceContractTest` 通过
- StrictMode 复测：目录遍历和过滤主线程栈已消失
- 连续 7 次热进程首帧：中位数 `107 ms → 73 ms`
- 快速进入/返回取消旧任务：通过
- App 全量单测：584 tests，0 failures，0 errors，1 existing skip
- `:app:assembleDebug`：通过
- 最终无探针 APK：AI App Bridge 重装、页面 tree、截图和快速进入/返回回归通过
- App 进程日志：未发现 `FATAL EXCEPTION`、`AndroidRuntime`、StrictMode 或文件导入异常
- decision: 阶段 2 通过，可以独立提交

## V8 速度阶段 1：复用相同参考章节的语义校准

### 根因

`V8PsbmtDetector` 对每个目标章都会重建同一组参考窗口，并重复计算
参考窗口之间的 BGE 余弦支持度。固定书籍的 128 目标章阶段中，
旧路径产生 `10809` 次语义内存命中；复用参考 bundle 后为 `864` 次，减少 `92.0%`。

本阶段只缓存 1 份完全相同的参考 bundle：

- 参考文本或 `windowSize/windowStride/minWindowChars` 改变时立即失效。
- 缓存仅保留参考窗口、已有向量引用和 `DoubleArray` 支持度，不随目标章数增长。
- IDF、identity sketch、当前章和后续章仍每次按原逻辑计算。

### 等价性

| 检查项 | 基线 | 优化版 | 结论 |
| --- | --- | --- | --- |
| 输入 | `analysis=264, targets=256, chars=1031714` | 相同 | 相同 |
| 结果计数 | `normal=84, wrong=172` | 相同 | 相同 |
| `contentDigest` | `6e04c246a1be6f21bf2ab037fb122039` | 相同 | 相同 |
| 规范化缓存 SHA-256 | `f5dbab99…f89808239f` | `f5dbab99…f89808239f` | 逐章完全一致 |

规范化时仅忽略 `createdAtMs` 和 reason 中的运行耗时 `ms=...`；
章节索引、状态、建议、置信度、evidence、fingerprints 均参与哈希。
三次优化版真机运行均得到同一 SHA。

### 速度与测试干扰

- 历史无监控缓存中同一批 256 章的逐章耗时和为 `155522 ms`；
  优化版无 `meminfo` 轮询时为 `148002 ms`，方向性改善 `4.84%`。
- 已经被前一阶段热过的后 128 章，三次优化样本比基线稳定快 `1.88%–2.66%`。
- 整本 epoch 仍为 `417–441 s`量级，没有证据支持显著的端到端缩短；
  串行章节取数和冷嵌入磁盘读取仍是主导项。
- 高频 `dumpsys meminfo` 会每约 2.3 秒触发一次 Android 显式 GC，因此带该轮询的
  整本耗时只用于内存安全边界，不再用于速度结论。

### 内存安全边界

高频采样下，基线最大 TOTAL PSS 为 `632855 KB`；两次优化版为
`511050 KB` 和 `564856 KB`。这证明本修改未造成新的整本内存峰值风险，
但由于样本数和显式 GC 干扰，不把 `10.7%–19.2%` 的差值归因为本阶段的内存收益。

### Gate

- 回归测试先失败后通过：`V8ReferenceSemanticCacheTest`
- 相同参考逐 bit 浮点校准等价：通过
- 参考文本/窗口配置失效：通过
- 单条目容量边界：通过
- `:source-engine:test`：通过
- App 全量单测：584 tests，0 failures，0 errors，1 existing skip
- `:app:assembleDebug`：通过
- 真机 FULL 三次运行：结果 SHA 全部一致，无崩溃
- decision: 阶段 1 通过；它是有界的 detector 微优化，整本端到端仍需下一阶段
